// import {  AxiosError } from "axios";

// export function getErrMsg(err: AxiosError): string {
//   if (err instanceof AxiosError)
//     return err.response.data || err.message;
//   return "something went wrong";
// }
